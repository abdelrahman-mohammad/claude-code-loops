{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(cat | jq -r '.tool_input.file_path // empty'); if [ -n \"$FILE\" ]; then npx prettier --write \"$FILE\" 2>/dev/null; fi; echo '{\"suppressOutput\": true}'"
          }
        ]
      }
    ]
  }
}
